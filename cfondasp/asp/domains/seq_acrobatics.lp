%*---------------------------------------------------------------------------------------------------------------
Dead end states
D1. Broken Leg (var0=0)
---------------------------------------------------------------------------------------------------------------*%
% deadEndGroup(0).
% deadEnd(0, 0, 0).

% %*---------------------------------------------------------------------------------------------------------------
% Undo actiontypes
% ---------------------------------------------------------------------------------------------------------------*%
% undo("walk-left", "walk-right").
% undo("walk-right", "walk-left").

% undo("climb", "climb-down").
% undo("climb-down", "climb").

%*---------------------------------------------------------------------------------------------------------------
Constraints
---------------------------------------------------------------------------------------------------------------*%
#program step(t). % t=1,2,...
:- policy(t, Action, Effect), actionType("jump-over", Action), actionEffect(Action, Effect).